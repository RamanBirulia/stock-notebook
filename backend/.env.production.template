# Production Environment Variables Template
# Copy this file to .env.production and fill in the actual values
# DO NOT commit the actual .env.production file to version control

# Database Configuration
# For Digital Ocean managed PostgreSQL:
DATABASE_URL=postgresql://username:password@host:port/database_name
# For SQLite (not recommended for production):
# DATABASE_URL=sqlite:stocks.db

# JWT Secret Key
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=teLzWxpHqg6uUoh4aFE/+/R96yLYdFt2DW5i96rSb6o=

# Frontend URL for CORS
# This should be your frontend domain
FRONTEND_URL=https://your-app-name-frontend.ondigitalocean.app

# Server Configuration
PORT=8080
RUST_LOG=info

# Optional: Custom domain if you have one
# FRONTEND_URL=https://yourdomain.com

# Optional: Database connection pool settings
# DATABASE_MAX_CONNECTIONS=20
# DATABASE_MIN_CONNECTIONS=5

# Optional: API rate limiting
# RATE_LIMIT_REQUESTS_PER_MINUTE=100

# Optional: Session timeout (in seconds)
# JWT_EXPIRATION_SECONDS=86400

# Security Headers (optional)
# ENABLE_SECURE_HEADERS=true
# ALLOWED_ORIGINS=https://yourdomain.com,https://your-app.ondigitalocean.app
